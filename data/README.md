# Datasets Directory

This directory stores datasets for ProX AI model training and evaluation.

- `smoke_test.jsonl`: Small synthetic dataset for local overfit smoke testing.
- User-provided training and validation datasets (`train.jsonl`, `valid.jsonl`).
