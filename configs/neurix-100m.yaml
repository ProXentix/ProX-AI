model:
  name: neurix-100m
  vocab_size: 32000
  d_model: 768
  n_layers: 12
  n_heads: 12
  d_ff: 1720
  max_seq_len: 2048
  tie_weights: true

training:
  batch_size: 1
  gradient_accumulation_steps: 16
  learning_rate: 3.0e-4
  weight_decay: 0.1
  warmup_steps: 500
  max_steps: 10000
  gradient_clip: 1.0
  precision: auto
  seed: 42

checkpoint:
  save_every: 500
  output_dir: ./weights/neurix
